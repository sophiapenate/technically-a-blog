<h3>Latests News:</h3>

<ul class="list-group list-group-flush">
  {{#each posts as |post|}}
    <li class="list-group-item py-3 px-4">
      <h4>
        <a href="/post/{{post.id}}">{{post.title}}</a>
      </h4>
      <p class="p-0 m-0">Posted by
        {{post.user.username}}
        on
        {{post.createdAt}}
        |
        {{post.comments.length}}
        {{format_plural "comment" post.comments.length}}</p>
    </li>
  {{/each}}
</ul>